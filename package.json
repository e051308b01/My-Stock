{
  "name": "my-stock-dashboard",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i src.css -o styles.css --minify",
    "check": "node --check app.js && node --check scripts/update-data.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.11",
    "tailwindcss": "4.1.11"
  }
}
